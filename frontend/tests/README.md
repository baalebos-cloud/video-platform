Reserved for frontend tests. No test runner is wired up yet — add
Vitest + React Testing Library (or Playwright for E2E) here as the UI
grows past what manual QA and the TypeScript compiler catch. The
backend's `tests/` directory (pytest) is the actively maintained test
suite in this repository today.
