# Deployment manifests

Platform-specific deployment manifests (Kubernetes, ECS task definitions,
Terraform, etc.) go here as the target platform is chosen. See
`docs/deployment.md` for the current Docker Compose-based deployment
and the production principles to carry into whichever manifest format
you add.
